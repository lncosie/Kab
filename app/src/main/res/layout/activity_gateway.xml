<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/HVFill"
    android:background="@color/lighter_gray"
    >
    <include layout="@layout/title_bar"></include>
    <!--<LinearLayout-->
        <!--android:gravity="center"-->
        <!--style="@style/HVFill">-->
        <TextView
            android:layout_gravity="center"
            android:layout_margin="@dimen/dp32"
            android:id="@+id/search_gateway"
            android:text="@string/search_gateway"
            android:gravity="center"
            android:textColor="@color/light_white"
            android:background="@drawable/shape_search_button"
            android:scaleType="centerInside"
            android:elevation="@dimen/dp4"
            android:layout_width="@dimen/dp80"
            android:layout_height="@dimen/dp80"></TextView>
        <ListView
            android:id="@+id/gateways"
            android:background="@color/white"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </ListView>
    <!--</LinearLayout>-->

</LinearLayout>
